# Scoring

Deterministic scoring and rules. Runs after the Claude analysis to
compute the per-child fit verdict.

Files:

- `hard-rails.ts` — certification age gating, age-from-DOB helper
- `fit.ts` — overall score computation, verdict picking, reasoning text
