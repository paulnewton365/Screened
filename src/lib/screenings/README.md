# Screenings

A screening is a parent's record of considering or watching a title for a
child. Created when a parent saves a title to the child's library; updated
later with observations after viewing.

Files:

- `schemas.ts` — Zod schemas for save and observation flows
- `actions.ts` — server actions: saveScreening, updateObservations, deleteScreening
