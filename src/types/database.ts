/**
 * Database types.
 *
 * This file is a minimal placeholder. To generate the real types from
 * your Supabase schema, run:
 *
 *   pnpm dlx supabase gen types typescript --project-id <your-project-ref> \
 *     > src/types/database.ts
 *
 * Replace this whole file with the output. Do this whenever you change
 * the schema (add a column, alter a type, etc.) so the types stay in sync.
 *
 * Until you generate the real types, the app will still work — we just
 * lose some autocomplete and type checking on database queries.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
